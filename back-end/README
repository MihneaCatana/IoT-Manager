########################################

Prereq:

- have node.js installed
- have docker desktop open

For starting the kafka containers, run command:

- docker compose up -d

Access Kafdrop:

localhost:9000
